<!DOCTYPE html>
<html>
  <head>
    <title>MasterTaxi - Gerenciador de Corridas</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "https://maps.googleapis.com/maps/api/js?key=#{Figaro.env.google_api}&libraries=places&callback=dispatchMapsEvent", 
                                                  async: true, 
                                                  defer: true, 
                                                  "data-turbolinks-eval": false %>
    <%= javascript_importmap_tags %>

    <meta name="theme-color" content="#ffffff">

  </head>

  <body>
    <div class="flex bg-gray-300">
      <div class="flex flex-col w-64 h-screen px-4 py-8 overflow-y-auto border-r bg-indigo-900 sticky top-0">
          <%= render '/shared/sidebar' %>
      </div>
      <div class="w-full h-full overflow-y-auto">
        <div class="flex flex-col items-center justify-center">
          <%= yield %>
        </div>
      </div>  
  </body>
</html>