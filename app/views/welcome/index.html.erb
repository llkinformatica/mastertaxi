<div>
  <h1 class="font-bold text-4xl">RANKING</h1>
  <p>Ranking atualizado em: <%= Time.now.strftime('%d/%m/%Y %H:%M:%S') %></p>

  <p class="text-[#e0ae00]">Diurno</p>
</div>

<div data-controller="hello" data-hello-hidden-class="hidden">
  <span class="hidden" data-hello-target="element">This toggles!</span>
  <button data-action="click->hello#show">Show</button>
  <button data-action="click->hello#hide">Hide</button>
</div>